% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statgl_plot.R
\name{statgl_plot}
\alias{statgl_plot}
\title{Create a highcharter plot with Statgl defaults}
\usage{
statgl_plot(
  df,
  x,
  y = value,
  type = NULL,
  name = NULL,
  group = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  show_last_value = FALSE,
  xlab = NULL,
  ylab = NULL,
  tooltip = NULL,
  suffix = "",
  digits = 0,
  big.mark = ".",
  decimal.mark = ",",
  locale = NULL,
  stacking = NULL,
  palette = "main",
  palette_reverse = FALSE,
  pyramid = NULL,
  height = 300,
  legend_position = "bottom",
  ...
)
}
\arguments{
\item{df}{A data frame.}

\item{x, y}{Bare column names for x and y aesthetics. \code{y} defaults to \code{value}.}

\item{type}{Optional string specifying the chart type. If \code{NULL}, the type is
inferred from the structure of \code{x}:
\itemize{
\item \code{"line"} if \code{x} is \code{Date}/\code{POSIXct} or an integer-like numeric with many
distinct values
\item \code{"column"} if \code{x} is a factor/character or numeric with few distinct
values
\item \code{"scatter"} otherwise.
}}

\item{name}{Optional series name passed to \code{\link[highcharter:hchart]{highcharter::hchart()}}.}

\item{group}{Optional bare column name used to split data into series
(\code{highcharter::hcaes(group = ...)}).}

\item{title, subtitle, caption}{Optional text annotations added via
\code{\link[highcharter:hc_title]{highcharter::hc_title()}}, \code{\link[highcharter:hc_subtitle]{highcharter::hc_subtitle()}} and
\code{\link[highcharter:hc_caption]{highcharter::hc_caption()}}. Titles and subtitles are left-aligned;
captions are right-aligned.}

\item{show_last_value}{Logical; if \code{TRUE} (default), adds data labels for the
final point of each \code{"line"}, \code{"spline"} and \code{"area"} series, or for all
bars in \code{"bar"} / \code{"column"} charts.}

\item{xlab, ylab}{Axis labels. If \code{NULL} or \code{""}, no axis title is shown and
any automatic titles inferred by \code{\link[highcharter:hchart]{highcharter::hchart()}} are disabled.}

\item{tooltip}{Optional JavaScript string passed to
\code{\link[highcharter:hc_tooltip]{highcharter::hc_tooltip()}} as a custom \code{formatter}. If \code{NULL}, a default
tooltip is used that respects \code{group}, \code{digits}, \code{big.mark},
\code{decimal.mark} and \code{suffix}.}

\item{suffix}{Character suffix appended to formatted values in data labels
and the default tooltip, e.g. \code{" \%"} or \code{" personer"}. Defaults to \code{""}.}

\item{digits}{Integer number of decimal places used in the default tooltip
and data labels. Defaults to \code{0}.}

\item{big.mark}{Character used as thousands separator for number formatting.
Defaults to \code{"."}.}

\item{decimal.mark}{Character used as decimal separator for number
formatting. Defaults to \code{","}.}

\item{locale}{Optional locale code (\code{"da"}, \code{"kl"}, \code{"en"}, etc.). If
provided and \code{big.mark} and \code{decimal.mark} are not explicitly overridden,
they are derived from \code{locale} (\code{"da"}/\code{"kl"} -> decimal \code{","}, big mark
\code{"."}; other values -> decimal \code{"."}, big mark \code{","}).}

\item{stacking}{Optional stacking mode for \code{"area"}, \code{"column"} and \code{"bar"}
charts. One of \code{"normal"} or \code{"percent"}, or \code{NULL} (no stacking).}

\item{palette}{Optional palette specification for the series colours. Either:
\itemize{
\item a single character name of an element in \code{statgl_palettes} (e.g.
\code{"main"}, \code{"winter"}, \code{"autumn"}), or
\item a character vector of colour hex codes to pass directly to
\code{\link[highcharter:hc_colors]{highcharter::hc_colors()}}.
}}

\item{palette_reverse}{Logical; if \code{TRUE}, reverse the palette when a named
Statgl palette is used. Ignored when \code{palette} is a vector of hex colours.
If the named palette is not found, a warning is issued and the default
Highcharts colours are used.}

\item{pyramid}{Pyramid layout for two-group charts (e.g. population
pyramids). Default \code{NULL} (or \code{FALSE}) draws a normal chart. Other forms:
\itemize{
\item \code{TRUE} -- enable pyramid mode using the column passed to \verb{group =} as
the splitting variable. Convention: men go on the right. Side ordering
is chosen by (a) a male-label heuristic
(\code{M}/\verb{M\\u00e6nd}/\code{Men}/\code{Angutit}/...) putting men on the right,
(b) a female-label heuristic putting women on the left, (c) factor
levels if the group column is a factor, or (d) first appearance
otherwise. When \code{TRUE}, the group column must resolve to exactly two
distinct values.
\item A single string such as \code{"M"} -- treated as the men/right value; the
other side is inferred from the data (the single remaining value, or
the one matching the female-label heuristic if more than one remains).
\item A length-2 character vector \code{c(left, right)} such as \code{c("K", "M")}
setting the sides explicitly.
}

When \code{pyramid} is a string or a length-2 vector, rows whose \code{group}
value isn't one of the named levels are silently dropped (useful for
PXWeb tables that include \code{"I alt"} / \code{"T"} totals alongside the two
sex codes). Series and legend order are then locked to pyramid order so
the legend reads left -> right with men on the right.

It composes with any \code{type}; if \code{type} is not supplied it defaults to
\code{"bar"} (rather than the usual \code{"line"} inference for integer ages).
For \code{"bar"} and \code{"column"}, \code{stacking} defaults to \code{"normal"} so the
two sides share the zero baseline; for \code{"area"}, \code{"line"} and friends
it is left unset, since each series is already drawn from baseline 0
and forcing stacking on a mixed-sign area chart causes Highcharts to
clip the negated side.

Pyramid always renders horizontally (categorical axis vertical, value
axis horizontal extending left/right of zero -- the conventional
orientation, equivalent to applying \code{ggplot2::coord_flip()}). For
\code{type = "bar"} Highcharts handles this automatically; for \code{"area"},
\code{"line"}, \code{"column"}, \code{"spline"} and \code{"areaspline"} it is achieved with
\code{chart.inverted = TRUE}. The x-axis is set to \code{reversed = FALSE} so
age 0 sits at the bottom. When \code{x} has more than ~30 distinct values
and \code{height} is not passed explicitly, height is scaled up to as tall as
is allowed.}

\item{height}{Numeric chart height in pixels passed to
\code{\link[highcharter:hc_chart]{highcharter::hc_chart()}}. Defaults to \code{300}.}

\item{legend_position}{Where to place the legend. One of \code{"top"},
\code{"bottom"} (default), \code{"left"}, \code{"right"}. Any other value (e.g.
\code{"none"}, \code{NULL}, \code{FALSE}) hides the legend.}

\item{...}{Additional arguments forwarded to \code{\link[highcharter:hchart]{highcharter::hchart()}}.
Names that collide with arguments \code{statgl_plot()} already sets
(\code{object}, \code{type}, \code{mapping}, \code{name}) are silently ignored so the
wrapper's own values win.}
}
\value{
A \link[highcharter:highchart]{highcharter::highchart} object.
}
\description{
\code{statgl_plot()} is a wrapper around \code{\link[highcharter:hchart]{highcharter::hchart()}} that provides
Statgl-friendly defaults for chart types, formatting, labels, colors, and
layout. It handles number formatting, suffixes, value labels, axis-title
suppression, grouped tooltips, stacking, colour palettes, and optionally
highlighting the last values in line/area charts.
}
